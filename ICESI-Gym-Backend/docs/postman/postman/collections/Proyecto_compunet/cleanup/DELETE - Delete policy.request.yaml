$kind: http-request
url: "{{baseUrl}}/api/v1/policies/{{testPolicyId}}"
method: DELETE
headers:
  Authorization: Bearer {{token}}
scripts:
  - type: afterResponse
    code: "pm.test(\"Status 204 on delete\", function () {\r

      \    pm.response.to.have.status(204);\r

      });\r

      pm.environment.unset(\"testPolicyId\");"
    language: text/javascript
order: 8000
